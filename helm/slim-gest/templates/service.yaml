apiVersion: v1
kind: Service
metadata:
  name: {{ include "slim-gest.fullname" . }}
  labels:
    {{- include "slim-gest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slim-gest.selectorLabels" . | nindent 4 }}
